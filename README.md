## Quick start

```bash
$ uv init
$ uv venv .venv
$ source .venv/bin/activate
$ uv pip install -e .
```

## Develepment
Sprint 1 (unblocks everything else): #19 (fix zero-lambda) → #12 (controls) → #13 (CIs) → #16 (theory consolidation)

Sprint 2 (core experimental depth): #15 (causal intervention) → #22 (06_C ↔ 06_B loop) → #21 (multi-seed) → #17 (ΔS histograms)

Sprint 3 (generality and polish): #14 (corpus) → #18 (L12 replication) → #20 (baselines) → #23 → #24 → #25